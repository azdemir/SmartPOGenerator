package test;

import org.jsoup.nodes.Element;

public class GeneratedElement {
    final Element element;
    final LocatorCandidate primaryLocator;
    final String fieldName;

    GeneratedElement(Element element, LocatorCandidate primaryLocator, String fieldName) {
        this.element = element;
        this.primaryLocator = primaryLocator;
        this.fieldName = fieldName;
    }
}
